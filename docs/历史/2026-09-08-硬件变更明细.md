# 2026-09-08 硬件变更明细（历史）

本文件从 [硬件台账](../硬件台账.md) 移出，仅作当日过程追溯。当前接线、轮位、PID、Odom 与 IMU 符号以台账「已确认」为准；测试命令与原始结果见 [测试记录](../测试记录.md)。

### 连接复核

- 当时只读检查：`/dev/myserial`、`/dev/ttyUSB0` 均不存在，USB 列表未枚举到控制板 `1a86:7523`；原有 `myserial.rules` 仍存在。不能据此判断控制板损坏，供电和 USB 接线待现场检查。
- 当时仅有 CP2102 `10c4:ea60` 串口 `/dev/ttyUSB1`，其稳定路径与历史雷达记录一致，不用于底盘驱动。
- M2/M4 是否已交换及架空安全条件仍待用户确认。本次未打开串口、未发送运动命令，既有轮位确认记录保持不变。
- 用户决定：不换线，通过软件适配轮位。替代此前换线计划；实物轮位沿用既有确认值，新增 `software_closed_loop_test.yaml`，验证状态为待实机。

### 控制板重新连接

- 用户反馈已连接；随后只读检查确认 CH340 `1a86:7523` 已枚举，`/dev/myserial -> ttyUSB0` 恢复，稳定路径为 `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`。
- `fuser` 未发现底盘串口占用，进程检查未发现底盘驱动或键盘测试进程。此前 USB 未枚举状态已解除。
- 尚未打开串口，串口收发、轮向和软件闭环效果仍待验证；四轮架空及电机急停条件待现场确认。

### 软件映射架空验收

- 用户确认四轮均按预期方向转动并可停止。前进的原始编码器符号稳定为 `[-,+,-,+]`，按已确认端口符号换算后四轮均为车轮前进方向；左横移换算后为 `[-,-,+,+]`，符合当时轮位映射。
- 直接 PWM 与临时 Jetson PI 在多次脉冲下各轮增量不均，带载不足；用户决定切换到控制板内置运动 PID。该决策不改变已确认的实车端口轮位，也不构成内置 PID 已通过的结论。

### 板载 PID 轮位探测

- 架空、低速短脉冲实测左横移编码器增量 `[+98,-88,-104,+96]`，逆时针旋转 `[+80,+100,-108,-107]`；按已确认编码器符号换算后分别符合当时轮位的目标轮向。
- 结论：不拆线，使用控制板内置 PID；尚待地面带载、停车、每圈计数和运动学标定。

### 板载 PID 前进否决

- 地面 `vx=+0.02 m/s`、`2.00 s` 实测：`car_type=1` 与临时 `car_type=2` 均导致 M1/M3 前进、M2/M4 反向；未调用持久化车型或 PID 写入接口。
- 结论：保持当时硬件接线时，板载 PID 不作为正式控制。转为逐轮软件编码器闭环。此否决已被后续 M2/M4 插头对调与 A/B 对调后的 Flash PID 取代，见台账当前事实。

### 高输出逐轮闭环架空验收

- 当时候选限制为 PWM `45%`，临时全量程 `4000 count/s`；2 秒前进、左横移、逆时针的原始编码器符号和增量均符合当时端口轮位，前进四轮绝对增量最大差约 3%。
- 此配置仅通过架空验收。带载起步、地面三方向、停止距离、输出轴每圈计数和里程标定均待确认，不能作为已通过的正式速度或里程参数。

### 高输出逐轮闭环地面前进

- 用户确认 `0.02 m/s`、2 秒前进后车体无明显偏航且可停车；四路编码器符号正确。
- 前进方向、带载起步和停车通过；四轮增量差约 24%，地面横移、旋转、速度一致性、停止距离及里程标定待验证。

### 验收证据修订与 IMU 条件确认

- 变更前：地面 Odom 描述为高精度通过。变更后：用户明确未保存读数、直行约 1 m，当前只确认初步观察正常；此前精确误差表述待复核，历史记录保留。
- 用户确认水平静止与无电机驱动的安全人工转动条件；尚未确认控制板固定状态，也未取得新的 IMU 数据。
- 影响范围：修订验收结论和下一步操作，不变更接线、PID、固件或正式参数。

### 控制板牢固固定确认与 IMU 单项测试启动

- 用户明确确认：小车能够水平静止，电机不驱动时可安全人工转动，控制板已牢固固定，具备 IMU 测试条件。
- 里程计验收状态维持“地面初步观察正常（直行约 1 m，未保存读数）”，不作高精度验收判定，但已完成的 10 圈编码器标定值有效保留，不必重测。
- 实施 IMU 单项测试第一阶段：隔离驱动 `imu_test.launch.py`（速度限制为 0、隔离 cmd_vel、关闭 Odom/TF）、采样脚本 `run_imu_stationary_check.sh` 及停止脚本 `stop_imu_test.sh`。
- 本轮不修改 PID、固件、正式运动学或正式配置 `base.yaml`，不进行 IMU 融合。

### IMU 单项实测完成（静止与逆时针旋转）

- 水平静止 30 秒实测（752 帧，25.07 Hz）：
  - 陀螺仪零偏：`gx=-0.000222, gy=+0.000031, gz=+0.000453 rad/s`，std 约 0.005 rad/s。
  - 加速度分量：`ax=+0.0412, ay=-0.0706, az=-9.8923 m/s²`。
- 水平缓慢逆时针旋转 15 秒实测（377 帧，25.13 Hz）：
  - 角速度：`gx=+0.0028, gy=-0.0025, gz=-0.1587 rad/s`（`gz` 峰值 `-0.7240 rad/s`）。
  - 主轴判定：旋转动态能量集中于 Z 轴，X/Y 无耦合，Z 轴确立为主轴（PASS）。
  - 符号判定：实测 `gz < 0`，与 ROS REP-103 冲突。`Rosmaster_Lib.py` 第 155 行存在 `*-gyro_ratio`。
  - 垂向加速度：`az ≈ -9.88 m/s²`，符号同为负。
- 处理决策：不接入 EKF，不在参数中掩盖；在驱动映射层显式校正（`gz_corrected = -gz_raw`, `az_corrected = -az_raw`）。后续已落地为 `imu_*_signs: [1, 1, -1]`，见台账。

### IMU 驱动层轴向符号校正落地

- 工程决策：在底盘驱动 ROS 封装层透明校正，不用 EKF 掩盖。
- 参数：`imu_gyro_signs: [1, 1, -1]`，`imu_accel_signs: [1, 1, -1]`；`validate_imu_signs` / `apply_imu_signs`。
- 影响：`math_utils.py`、`rosmaster_node.py`；`base.yaml`、`imu_test.yaml`、`wheel_odometry.yaml`；41 项单元测试通过。

### NAV-001 SLAM Toolbox 建图链路实机冒烟通过

- 三终端独立启动，不改正式 `robot.launch.py`，不启动 Nav2。
- `slam_toolbox.yaml`：`min_laser_range: 0.10`、`max_laser_range: 3.00`、`scan_buffer_maximum_scan_distance: 3.0`；`odom_frame: odom`、`map_frame: map`、`base_frame: base_footprint`、`scan_topic: /scan`、`mode: mapping`。
- 只读工具：`scripts/check_slam_link.py` / `check_slam_link.sh`，严禁发布 `/cmd_vel`。
- 实机冒烟（PASS），日志 `log/slam_link_check_20260908_210938.log`：
  - 四节点存活独占：`rosmaster_base`、`robot_state_publisher`、`sllidar_node`、`slam_toolbox`；
  - `/scan` 11.0 Hz、`/wheel/odometry` 31.7 Hz、`/map` 112x64（空闲 2339、占用 142）；
  - `map -> odom -> base_footprint -> base_link -> laser_frame` 贯通，无持续 TF 超时或丢帧。
