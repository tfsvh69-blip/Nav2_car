# 硬件台账

最后更新：2026-09-17

本文件只保留当前配置与未确认事实。原始测试过程与早期记录见[测试记录](测试记录.md)。

## 已确认

| 类别 | 当前事实 |
|---|---|
| 计算平台 | Jetson Orin NX，Ubuntu 22.04 / ROS 2 Humble，使用原装电源适配器。 |
| 控制板 | Yahboom Rosmaster，控制板实读版本 V3.6，Python 库 V3.3.9，`car_type=1` （X3）。 |
| 控制板串口 | `/dev/myserial -> /dev/ttyUSB1`，115200，CH340/CH341，当前用户已具备串口读写权限。 |
| 声光指示器协议与接口（已确认） | 实机通过 (PASS)。Yahboom Rosmaster 协议驱动库 V3.3.9 支持蜂鸣器 `FUNC 0x02`（`set_beep(on_time)`：0 关闭，>=10ms 十倍数定时自动关闭，常鸣值 1 严禁使用）；外接全彩 RGB 灯带 `FUNC 0x05`（`set_colorful_lamps(id,r,g,b)`：单灯 0~13，255 全部灯，分量 0~255）。现场实测确认蜂鸣器与 RGB 驱动成功，低亮度 `30/255` 稳定工作。已实现 C++ `navigation_indicator` 节点驱动，底盘节点 `rosmaster_node.py` 通过 `/hardware/indicator/command` 严格隔离转发。 |
| 电机供电 | 板载闭环验收使用 12 V 电机主供电；开关必须可触及。 |
| 电机和编码器端口轮位（当前） | 用户已整体交换 M2/M4 一体插头，电机供电和编码器线同时交换：M1=左前、M2=左后、M3=右前、M4=右后。各通道的电机与编码器仍属于同一只轮。四台电机各自的编码器 A/B 线已经交换；用户反馈 PID 现与对应电机匹配并已恢复闭环驱动。 |
| 编码器每圈计数与正方向（已确认） | 2026-09-08 实机手转 10 圈实测：M1=1319.2、M2=1317.9、M3=1319.2、M4=1319.2 count/rev（与理论 1320 误差 <0.1%）；前进方向 Δ 均为正，四轮计数符号均为 `+1`。配置 `wheel_odometry.yaml` 已填入并启用 Odom。 |
| 板载 PID 与当前接线 | 控制板可逐路 PWM 和四路独立速度 PID；当前 ROS 节点 `drive_mode=board_motion_pid`，调用 `set_car_motion(vx,vy,wz)`，PID 在控制板内部运行。2026-09-08 用户实机 RQT 调参后确定全局运动 PID 为 `Kp=8.0, Ki=1.2, Kd=0.8`，已通过 `set_pid_param(forever=True)` 写入 Flash；用户随后确认 PID—电机对应问题已解决。V3.6 固件映射补丁保留但不刷写。 |
| 临时验收接口 | 软件映射＋`set_motor()`：架空轮向、编码器反馈、停车通过；PWM／临时 PI 带载不足，不作为正式控制。 |
| 当前 Odom 与运动模型 | 整车加装 Jetson、雷达、相机及支架后整车较重，麦轮微型 45° 辊子受重压滑动阻力大导致纯横移打滑跑偏；底盘退化并锁定为标准非全向差速模型（`holonomic=false`, `max_linear_y=0.0`），仅使用直行（前进/后退）与原地自转/弧线。`rosmaster_node` 直接读取四路累计编码器解算 `x` 与 `yaw` 并发布 `/wheel/odometry` 及 `odom -> base_footprint`。 |
| 纯轮式 Odom 地面基线（已确认） | 累计编码器 10 圈实测为 M1=1319.2、M2=1317.9、M3=1319.2、M4=1319.2 count/rev（全为 +1 正计数）。地面前进 1 m、原地 360° 自转与静止 30 秒零漂移通过。正式配置 `base.yaml` 已启用发布。 |
| IMU 标定与符号校正（已确认） | 控制板板载 MPU9250。30 秒水平静止实测角速度零偏均值绝对值 <0.0005 rad/s，std 约 0.005 rad/s；15 秒逆时针自转确立 Z 轴为主轴且无交叉耦合。原厂库反向符号已在驱动层配置 `imu_gyro_signs: [1, 1, -1]` 与 `imu_accel_signs: [1, 1, -1]` 修正符合 ROS REP-103 规范，41 项单元测试全通。本阶段暂不接入融合。 |
| 底盘几何 | 轮径 60 mm，轮宽 31 mm，轴距 120 mm，轮距 185 mm。 |
| 整车外廓 | 长 235 mm，建模宽 216 mm，高 230 mm，最小离地 10 mm。 |
| 顶板 | `220×130×10 mm`，顶面离地 140 mm。 |
| 雷达 | RPLIDAR A1（串口 `/dev/ttyUSB0`，115200，CP2102），扫描中心相对 `base_footprint=(-50,0,217) mm`，RViz 平面方向已通过。标准 XML 启动入口为 `carcar_lidar/launch/lidar.launch.xml`，`carcar_bringup` 提供兼容器；保留历史 `lidar.launch.py`。 |
| 整车模型与固定 TF 发布入口 | 标准入口为 `carcar_description/launch/description.launch.xml`，由唯一 `robot_state_publisher` 从现有 Xacro 发布 `/robot_description` 与固定 TF；`carcar_bringup/launch/robot.launch.xml` 将该入口与底盘驱动组合。离线验证通过，修复后的 RViz/AMCL 静止实机复验待确认。 |
| 深度相机 | Intel RealSense D435，左红外中心相对 `base_footprint=(130,33,170) mm`，倒装 roll=`pi`。 |
| D435 状态 | 外部主机四路成像通过；Jetson `640×480 @ 6 FPS` RGB+深度目视通过。 |
| IMU 安装位姿 | 位置暂估为 `base_footprint=(-40,-15,75) mm`，驱动层已完成符号校正，不参与当前融合。 |
| SLAM 建图链路（NAV-001 已确认） | 纯轮式 Odom + RPLIDAR A1 + SLAM Toolbox 2.6.10 异步建图链路已贯通。实测 `/scan`（11.0 Hz，`laser_frame`，有效点 596/720）、`/wheel/odometry`（31.7 Hz）、`/map`（0.050 m/cell，112x64 栅格）及 `map -> odom -> base_footprint -> base_link -> laser_frame` 全 TF 树连通且无持续告警，原地静止冒烟实机通过。 |
| SLAM 扩大建图与新地图（NAV-004 已确认） | 实机通过 (PASS)。2026-09-14 建立覆盖大范围的新地图 `nav004_20260914_093032`，尺寸 `259 x 230`（较旧版扩大 4.5 倍），分辨率 `0.05 m/cell`，原点 `[-8.83, -5.90, 0]`；已探明空闲区域 56269 格，占据障碍 3301 格；成对保存 YAML/PGM 文件并通过 C++ `check_map_reload` 动态基线四项指标独立重载验证（退出码 0）。 |
| Nav2 单点导航与调优（NAV-003/005 已确认） | 首个单点导航曾到达目标；后续现场日志同时出现 `No valid trajectories`、`Failed to make progress` 与扫描时刻 TF 过期后的 `Controller patience exceeded`。现有稳定入口保持为对照，不能再把避障失败归因于单一参数或宣称已稳定通过。 |
| Nav2 差速单点导航底座（NAV-003） | 离线验证通过 (PASS)。ROS 2 Humble Nav2 组件版本 1.1.20-1jammy（Apache-2.0）；AMCL 锁定 DifferentialMotionModel；DWB 与平滑器锁定为单向前进与旋转，锁定 min_vel_y=max_vel_y=0.0、min_vel_x=0.0，禁用横移与倒车；平滑器输入超时 0.20 s，底盘保留 0.30 s 硬件看门狗；专用单目标无恢复行为树已就绪。 |
| 导航代价膨胀与包络（NAV-010 实验值） | 现场反馈证实此前文档（称“全局/局部均为 0.35m”）与代码实际（代码中全局关闭 enabled: false，局部 0.15m）脱节；关闭全局膨胀导致规划路径紧贴障碍边缘（中心线净空 0cm），宽 0.26m 车身无法通过并触发控制器碰撞淘汰。现已校准：全局膨胀正式开启（`enabled: true, inflation_radius: 0.30 m, cost_scaling_factor: 5.0`）；局部膨胀维持 `0.15 m / 5.0`；车身安全包络锁定 `0.28×0.26 m`。膨胀半径表示代价影响范围，不等于保证车身净距。 |
| Groot 1 与状态桥接（已停用） | 曾使用固定提交 `70973d0` 构建。由于实时维护成本高且缺乏历史追溯力，NAV-010/NAV-011 起已正式停用并默认关闭启动，全面改由终端中文状态摘要与后台自动多维度会话录包（`nav_event_logger` + `nav_log_tool`）替代。保留相关文件仅供历史对照。 |

## 待确认

1. NAV-003 静态地图 AMCL 原地定位收敛、1m 直线与 90° 转向单点到点精度、取消停车、命令超时安全及全路径障碍阻断实机验收。
2. DWB 与速度平滑器加减速初始设定值（线加减速 0.20 m/s²、角加减速 0.40 rad/s²）在地面带载运行时的实际响应与起步死区。
3. MOTOR-017 逐轮 50% PWM 正转的完整观察记录（非建图阻塞项；10 圈计数已完成并给出方向）。
4. 纯轮式 Odom 量化转角重复性复核（当前初步观察正常，已满足建图输入要求）。
5. D435 的绝对深度精度、高帧率长时稳定性。Jetson 原生 UVC 仍有 `-32/-71` 日志，暂不将其当作高负载通过。
6. 电池化学类型、容量、充电判据与低电阈值。
7. NAV-007 初始偏差、途中失配、恢复超时与重复结构下的定位成功率；NAV-010 左右绕行各五次、封路、障碍撤走、后方受阻与受限倒车结果。

## 最近变更

| 日期 | 变更 | 影响/状态 |
|---|---|---|
| 2026-09-17 | NAV-012-R1 路径更新孤儿时限、定位健康回调与声光 5 模式 | 未改变硬件、固件、尺寸、系统配置或 IMU 融合。软件离线补丁：FollowPath 位姿更新保持许可、戳记变化不重发、`bt_tick_age_s`、健康门控共用 RecoveryRuntime 执行器、操作员灯光 5 模式青色 1/3 亮度 (0,85,85)。离线验证通过，修复版实机待验收。完整操作见手册第 17、18 节。 |
| 2026-09-17 | NAV-012-R1 软件修复及 2026-09-16 现场结论修订 | 未改变硬件、固件、尺寸、系统配置或 IMU 融合。导航速度联锁和恢复监督已完成离线验证，修复版待实机。原 12.40 V 与定位门控 true 是现场读数；`watchdog_stopped` 也会因零速置真，不能单独证明超时停车；旧目标终态曾污染当前状态，红灯不能独立证明最新目标失败。此前“看门狗已确认停车原因”和红灯精确归因的表述以本修订为准。完整操作见手册第 18 节。 |
| 2026-09-16 | NAV-012 实机导航恢复失败与红灯现场反馈 | 未改变硬件。定位监控一直就绪，电压诊断为 12.40V，底盘看门狗已确认小车静止。后灯低亮度红色对应 `NAVIGATION_FAILED/ABORTED`，不是低压或定位丢失指示。实机证据将失败定位到 NAV-012 自定义恢复节点的异步回调处理缺陷；待软件修复后再实测。 |
| 2026-09-16 | TF-004 整车模型与实验定位启动链路修复 | 现场确认 2D Pose Estimate 已被 AMCL 接收，但旧底盘入口遗漏 `robot_state_publisher`，导致模型、`/robot_description`、固定 TF 和雷达定位链路缺失；实验导航又与不含 `localization_monitor` 的普通定位入口混用。新增 XML 模型入口和整车组合入口，现行流程统一配套实验定位与实验导航。未改接线、模型尺寸、传感器外参、电机、定位或导航参数；167 项离线测试无失败，待静止实机复验。 |
| 2026-09-16 | NAV-014 导航声光提示与停车原因诊断系统落地 | 纯软件状态机与声光指示驱动。实现 C++ navigation_indicator 节点与 carcar_interfaces/IndicatorCommand 消息；覆盖 14 阶段与 17 类停车原因低亮度 (30/255) 声光映射，有效停车 3.0s 周期 (2.0s 停车原因/1.0s 阶段) 交替显示；2.0s 通信超时紫灯快闪看门狗；蜂鸣器单次触发与去重降噪；现有 Python 底盘驱动 rosmaster_node.py 严格隔离转发，超时 1.0s 自动关停且具备零运动安全保证。全套 166 项单元与回归测试 100% 通过。 |
| 2026-09-15 | DRV-004 声光指示器测试与协议驱动落地 | 纯底层 C++ 驱动与测试工具。实现底层指示器通信协议（FUNC 0x02, 0x05, 0x06）及独立测试节点/XML Launch。包含零速度安全防护、flock 串口独占保护、dry-run 离线模拟与实车 100ms 蜂鸣/RGB 低亮度单色（各 1s）时序执行。离线验证全通，待实车现场观察。 |
| 2026-09-15 | NAV-013 导航状态日志完善与停车阻断归因升级 | 纯软件日志与诊断增强。规范行为树节点类型识别（ProgressGuard, ControlledSpin, ParkAndObserve, BackUp 0.10m/0.05m/s/4.0s）；新增 PARK_AND_OBSERVE 阶段；支持 NavigateToPose 与 NavigateThroughPoses 全流程 UUID；发布只读 /navigation/status 诊断阵列；对比双层速度；实现纯轮式里程计静止（<0.01m/s & <0.02rad/s 1.0s）与数据过期（>0.6s）判定；建立多维停车归因定级（禁止单一归因）；nav_log_tool 扩展 --stops 与 --failures；平滑兼容历史会话。全套 130 项测试通过。 |
| 2026-09-14 | NAV-012 墙角绕行、进展停滞识别与有序脱困实验落地 | 纯软件配置与恢复逻辑。保持纯轮式里程计与 0.28x0.26m 安全包络不变；实现 RotationShimController 包裹 DWB（误差>0.60rad 原地对齐，<=0.25rad 退出）、ProgressGuard（5s/0.03m 与 5s/0.10rad 停滞判定）、RearClear 异步读取与致命障碍/膨胀区分、ProtectedBackUp（单次 0.10m，累计限额 0.30m，巡航充能）、ControlledSpin（<=90度）与 ParkAndObserve（30s 周期轮询）。59 项单元测试全通。待实车验收。 |
| 2026-09-14 | NAV-010 全局膨胀层开启与垃圾桶绕障路径净空校准 | 纯软件配置修改。修正文档与代码配置脱节问题，全局膨胀由关闭改为开启（`0.30m/5.0`），局部膨胀（`0.15m/5.0`）与车身包络（`0.28x0.26m`）保持不变。合成障碍离线验证通过，路径中心线净空由 0cm 提升至 >0.13m，包络无碰撞。待实车左右各 5 次绕障验收。 |
| 2026-09-14 | NAV-011 终端状态摘要、多维度会话录包系统落地与 Groot 停用 | 纯软件工具与日志扩展。默认关闭并停用 Groot 与桥接节点；`nav_event_logger` 新增 1Hz 终端中文摘要、阶段切换即时告警、`RearClear` 6 类拒绝诊断及 `log/nav_sessions/` 自动录包（JSONL/文本/1GB分卷rosbag2/10GB限额保护）；`nav_log_tool` 扩展会话清单、时间线、故障汇总、纯离线回看与归档导出。 |
| 2026-09-14 | NAV-009 行为树插件 WaitForLocalizationStatus 落地与 Groot 桥接修复 | 纯软件接口修复，未修改硬件接线、电机、底盘或速度包络。修复实验树话题接口与桥接节点端口，解决 bt_navigator inactive 与 Groot 1666/1667 服务缺失，7 项 C++ 自动化回归测试通过。 |
| 2026-09-14 | NAV-007/NAV-008 实验入口与 Groot 1 用户目录安装 | 未改动接线、底盘、串口或系统配置。新增的导航值全部为临时实验值，Groot 桌面显示待实机验收。 |
| 2026-09-14 | NAV-004 扩大建图与新地图重载通过 | 实机通过 (PASS)。新地图 `nav004_20260914_093032`（259x230@0.05m，原点[-8.83,-5.90]）成对生成，C++ 校验工具动态比对四项指标全部通过（退出码 0）。 |
| 2026-09-14 | Nav2 虚拟包络校准与看门狗/动力优化 | 实机调优。针对原地 180° 折返触发假碰撞及 10s 卡死，包络校准为贴合实车的 `0.28x0.26m`；停滞超时放宽至 60s；旋转上限提至 0.60 rad/s、角加速度提至 0.80 rad/s²；局部代价衰减因子调至 10.0；DWB 朝向权重减半。 |
| 2026-09-09 | NAV-002 低速运动建图与地图保存通过 | 实机通过 (PASS)。直行 1.0 m、自转 360°、回访闭环误差 3.77 cm，地图保存成对生成。 |
| 2026-09-09 | NAV-003 Nav2 差速导航底座与独立入口就绪 | 离线验证通过 (PASS)。新增 localization/navigation XML Launch、navigation.rviz、无恢复行为树、DWB 差速非全向适配与 C++ 地图重载/链路诊断工具。待现场实机测试。 |
| 2026-09-10 | NAV-003 全局与局部膨胀层参数固化 | 用户现场选择 `inflation_radius=0.18 m`、`cost_scaling_factor=8.0`，未知区域膨胀关闭；已写入 `nav2.yaml`，待重启导航链路后进行实际路径与旋转方向复核。 |
| 2026-09-10 | NAV-003 导航速度上限提高 | 用户要求将导航前进和原地旋转上限提高 1.8 倍，永久配置为 `0.18 m/s` 与 `±0.36 rad/s`；DWB 与速度平滑器限制已同步，RQT 可实时调节。实际带载效果待现场验收。 |
| 2026-09-07 | D435 实机倒装修正 | 诊断显示旋转 180°，URDF `camera_link` roll=`pi`。 |
| 2026-09-07 | 底盘从开环 PWM 切换至板载闭环 | 待 M2/M4 接线修正和 MOTOR-007 实车验收。 |
| 2026-09-08 | 四台电机的编码器 A/B 线分别全部交换 | 用户已完成实物改线；闭环效果尚未验证。用户选择用连续低速键盘直接观察；首次仍必须架空确认不持续升速且停车有效，之后才能落地。 |
| 2026-09-08 | MOTOR-015 地面候选速度提高 | `0.05 m/s` 地面起步稳定并向前，无乱转或拉满。独立地面配置上限提高到直线/横移 `0.15 m/s`、旋转 `0.50 rad/s`，下一轮先用 `0.10/0.30` 分级测试；不覆盖原 `0.02/0.10` 低速入口。 |
| 2026-09-08 | 板载 PID 在线临时调谐入口 | 新增 RQT 暂存/停车提交/回读确认，不写 Flash；新增五档键盘和四路 count/s 曲线。当前全局 PID 记录值 `[0.8,0.06,0.5]`，用户反馈带载掉速明显，尚未实机改变参数。 |
| 2026-09-08 | 实机 PID 调参写入 Flash | 用户通过 RQT 实机调参确定 `Kp=8.0, Ki=1.2, Kd=0.8`，`set_pid_param(forever=True)` 写入控制板 Flash。键盘扩展至 9 档（最高 0.45 m/s），PID 验证范围放宽至 `[0,10]`。 |
| 2026-09-08 | 逐轮正向动作确认测试（MOTOR-017） | 提供 `scripts/verify_motor_directions.py`，按左上(M1)->右上(M3)->左下(M2)->右下(M4)以 +50% PWM 正转 2s，待实机记录各轮物理转向。 |
| 2026-09-08 | 进入纯轮式 Odom | 用户确认 PID—电机对应问题已解决；新增编码器 Odom 参数模板与启动入口，必须先完成四轮 10 圈实测，才允许启用 Odom/TF。 |
| 2026-09-08 | 四轮手转 10 圈计数标定完成 | 实测四轮均为前进正计数（符号全为 +1），M1=1319.2、M2=1317.9、M3=1319.2、M4=1319.2 count/rev。已写入 `wheel_odometry.yaml` 并正式启用 Odom 与 TF。待实车 1 m / 360° 验收。 |
| 2026-09-08 | 底盘切换为非全向差速模型 | 用户实机测试确认整车载重较重，麦轮辊子重载静摩擦大，横移打滑失效；正式决定停用横移，配置 `holonomic=false, max_linear_y=0.0`，底盘按前进/后退/自转模式运行，以保障高可靠性与高精度 Odom。 |
| 2026-09-08 | 纯轮式 Odom 地面三项实测通过 | 实车地面前进 1 m 物理与 Odom 接近一致，原地 360° 自转角度闭合误差仅 0.25°，静止 30 秒零漂移。正式配置 `base.yaml` 已开启 `publish_odom_tf` 与 `wheel_odometry_enabled`。ODOM-001 正式通过。 |
| 2026-09-08 | IMU 单项测试启动与控制板固定确认 | 用户确认控制板牢固固定、具备水平静止与安全人工转动条件。新增隔离驱动 imu_test 与配套脚本，进入第一阶段 30 秒静止数据采样。 |
| 2026-09-08 | IMU 单项测试完成（静止与逆时针旋转） | 30s 静止实测角速度零偏极小（各轴均值 <0.0005 rad/s，std 约 0.005 rad/s）；15s 逆时针自转实测 gz 响应显著（均值 -0.1587，峰值 -0.7240 rad/s），确立 Z 轴为主轴且无交叉耦合。定位到 vendor lib 强制乘以 -gyro_ratio 导致 gz 反向，az 同为负值。事实已确立，待驱动层取反，严禁用 EKF 掩盖。 |
| 2026-09-08 | IMU 驱动层轴向符号校正落地 | 在 math_utils 与 rosmaster_node 中新增 imu_gyro_signs 与 imu_accel_signs 参数，配置为 [1, 1, -1]，将 gz 与 az 修正为符合 ROS REP-103 规范（逆时针 gz>0、静止 az>0）。配置同步至 base.yaml、imu_test.yaml 与 wheel_odometry.yaml，41 项单元测试全通。 |
| 2026-09-08 | NAV-001 SLAM Toolbox 原地静止建图链路实机通过 | 三终端独立启动无冲突，四节点存活，单发布者独占，/scan=11.0Hz，Odom=31.7Hz，/map=112x64，TF 链完全贯通无告警，静止冒烟实机通过。 |

### 2026-09-08 连接复核

- 当前主机只读检查：`/dev/myserial`、`/dev/ttyUSB0` 均不存在，USB 列表未枚举到控制板 `1a86:7523`；原有 `myserial.rules` 仍存在。不能据此判断控制板损坏，供电和 USB 接线待现场检查。
- 当前仅有 CP2102 `10c4:ea60` 串口 `/dev/ttyUSB1`，其稳定路径与历史雷达记录一致，不用于底盘驱动。
- M2/M4 是否已交换及架空安全条件仍待用户确认。本次未打开串口、未发送运动命令，既有轮位确认记录保持不变。

- 2026-09-08 用户决定：不换线，通过软件适配轮位。替代此前换线计划；实物轮位沿用既有确认值，新增 `software_closed_loop_test.yaml`，验证状态为待实机。

### 2026-09-08 控制板重新连接

- 用户反馈已连接；随后只读检查确认 CH340 `1a86:7523` 已枚举，`/dev/myserial -> ttyUSB0` 恢复，稳定路径为 `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`。
- `fuser` 未发现底盘串口占用，进程检查未发现底盘驱动或键盘测试进程。此前 USB 未枚举状态已解除。
- 尚未打开串口，串口收发、轮向和软件闭环效果仍待验证；四轮架空及电机急停条件待现场确认。

### 2026-09-08 软件映射架空验收

- 用户确认四轮均按预期方向转动并可停止。前进的原始编码器符号稳定为 `[-,+,-,+]`，按已确认端口符号换算后四轮均为车轮前进方向；左横移换算后为 `[-,-,+,+]`，符合当前轮位映射。
- 直接 PWM 与临时 Jetson PI 在多次脉冲下各轮增量不均，带载不足；用户决定切换到控制板内置运动 PID。该决策不改变已确认的实车端口轮位，也不构成内置 PID 已通过的结论。

### 2026-09-08 板载 PID 轮位探测

- 架空、低速短脉冲实测左横移编码器增量 `[+98,-88,-104,+96]`，逆时针旋转 `[+80,+100,-108,-107]`；按已确认编码器符号换算后分别符合当前轮位的目标轮向。
- 结论：不拆线，使用控制板内置 PID；尚待地面带载、停车、每圈计数和运动学标定。

### 2026-09-08 板载 PID 前进否决

- 地面 `vx=+0.02 m/s`、`2.00 s` 实测：`car_type=1` 与临时 `car_type=2` 均导致 M1/M3 前进、M2/M4 反向；未调用持久化车型或 PID 写入接口。
- 结论：保持现有硬件接线时，板载 PID 不作为正式控制。转为逐轮软件编码器闭环；里程计与状态估计继续等待底盘地面控制闭环通过。

### 2026-09-08 高输出逐轮闭环架空验收

- 当前候选限制为 PWM `45%`，临时全量程 `4000 count/s`；2 秒前进、左横移、逆时针的原始编码器符号和增量均符合当前端口轮位，前进四轮绝对增量最大差约 3%。
- 此配置仅通过架空验收。带载起步、地面三方向、停止距离、输出轴每圈计数和里程标定均待确认，不能作为已通过的正式速度或里程参数。

### 2026-09-08 高输出逐轮闭环地面前进

- 用户确认 `0.02 m/s`、2 秒前进后车体无明显偏航且可停车；四路编码器符号正确。
- 前进方向、带载起步和停车通过；四轮增量差约 24%，地面横移、旋转、速度一致性、停止距离及里程标定待验证。

### 2026-09-08 验收证据修订与 IMU 条件确认

- 变更前：地面 Odom 描述为高精度通过。变更后：用户明确未保存读数、直行约 1 m，当前只确认初步观察正常；此前精确误差表述待复核，历史记录保留。
- 用户确认水平静止与无电机驱动的安全人工转动条件；尚未确认控制板固定状态，也未取得新的 IMU 数据。
- 影响范围：修订验收结论和下一步操作，不变更接线、PID、固件或正式参数。

### 2026-09-08 控制板牢固固定确认与 IMU 单项测试启动

- 用户明确确认：小车能够水平静止，电机不驱动时可安全人工转动，控制板已牢固固定，具备 IMU 测试条件。
- 里程计验收状态维持“地面初步观察正常（直行约 1 m，未保存读数）”，不作高精度验收判定，但已完成的 10 圈编码器标定值有效保留，不必重测。
- 实施 IMU 单项测试第一阶段：提供隔离驱动 `imu_test.launch.py`（速度限制为 0、隔离 cmd_vel、关闭 Odom/TF）、采样脚本 `run_imu_stationary_check.sh` 及停止脚本 `stop_imu_test.sh`。
- 本轮不修改 PID、固件、正式运动学或正式配置 `base.yaml`，不进行 IMU 融合。待现场获取实测六轴数据后评估零偏与噪声。

### 2026-09-08 IMU 单项实测完成（静止与逆时针旋转）

- 水平静止 30 秒实测（752 帧，25.07 Hz）：
  - 陀螺仪零偏：`gx=-0.000222, gy=+0.000031, gz=+0.000453 rad/s`，std 约 0.005 rad/s。零偏表现极佳。
  - 加速度分量：`ax=+0.0412, ay=-0.0706, az=-9.8923 m/s²`。车身水平度良好。
- 水平缓慢逆时针旋转 15 秒实测（377 帧，25.13 Hz）：
  - 角速度：`gx=+0.0028, gy=-0.0025, gz=-0.1587 rad/s`（`gz` 峰值达到 `-0.7240 rad/s`）。
  - 主轴判定：旋转动态能量完全集中于 Z 轴，X/Y 轴无耦合，Z 轴确立为主轴（通过 PASS）。
  - 符号判定：实测 `gz < 0`，与 ROS REP-103 向上逆时针为正冲突。查证 `Rosmaster_Lib.py` 第 155 行存在 `*-gyro_ratio` 负号处理。
  - 垂向加速度：`az ≈ -9.88 m/s²`，符号同为负。
- 处理决策：按 AGENTS.md 约定，不接入 EKF，不在参数中掩盖，后续在底盘驱动映射层或安装 TF 中显式做符号校正（`gz_corrected = -gz_raw`, `az_corrected = -az_raw`）。

### 2026-09-08 IMU 驱动层轴向符号校正落地

- **工程决策**：依据 ROS REP-103 规范与 AGENTS.md 第八条，在底盘驱动 ROS 封装层透明校正 IMU 轴向符号，不将反向问题留给上层算法或用 EKF 参数掩盖。
- **参数化设计**：
  - `imu_gyro_signs: [1, 1, -1]`
  - `imu_accel_signs: [1, 1, -1]`
  - 驱动加载时执行 `validate_imu_signs` 严格校验（必须为 3 个非零整数），发布时通过 `apply_imu_signs` 分别校正三轴数据。
- **影响范围**：
  - 核心逻辑：`src/carcar_base/carcar_base/math_utils.py`、`src/carcar_base/carcar_base/rosmaster_node.py`；
  - 配置文件：`src/carcar_base/config/base.yaml`、`src/carcar_base/config/imu_test.yaml`、`src/carcar_base/config/wheel_odometry.yaml`；
  - 单元测试：`src/carcar_base/test/test_math_utils.py`、`src/carcar_base/test/test_rosmaster_node_pid.py`（41 项测试全部通过）。

### 2026-09-08 NAV-001 SLAM Toolbox 建图链路实机冒烟通过

- **架构策略**：保持底层底盘、激光雷达与 SLAM 算法三者解耦，采用三个独立终端启动，防止串口抢占；不改动正式 `robot.launch.py`，不启动 Nav2。
- **参数校准**：
  - 目标文件：`src/carcar_navigation/config/slam_toolbox.yaml`；
  - 关键修改：`min_laser_range: 0.10`、`max_laser_range: 3.00`、`scan_buffer_maximum_scan_distance: 3.0`，严格限定在雷达实测有效范围（0.10～3.00 m）；
  - 接口保持：`odom_frame: odom`、`map_frame: map`、`base_frame: base_footprint`、`scan_topic: /scan`、`mode: mapping`。
- **只读诊断工具**：
  - 新增 `scripts/check_slam_link.py` 与 `scripts/check_slam_link.sh`；
  - 全程零驱动输出（严禁发布 `/cmd_vel`），只读校验节点存活、单发布者独占、消息帧、更新频率及完整 TF 树。
- **实机冒烟结论（PASS）**：
  - 2026-09-08 现场执行 `./scripts/check_slam_link.sh`（日志 `log/slam_link_check_20260908_210938.log`）；
  - 四节点全部存活独占：`rosmaster_base`、`robot_state_publisher`、`sllidar_node`、`slam_toolbox`；
  - 话题与消息帧正常：`/scan` (11.0 Hz, `laser_frame`)、`/wheel/odometry` (31.7 Hz, `odom` -> `base_footprint`)、`/map` (112x64 栅格，已探明空闲 2339、占用 142)；
  - 坐标系完全贯通：`map -> odom -> base_footprint -> base_link -> laser_frame`，静态与动态变换正常，无断链，无持续 TF 超时或丢帧。建图输入链路实机正式通过。
