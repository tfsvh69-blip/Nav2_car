# 导航巡检小车项目协作约定

本文件是当前仓库的长期开发约束。后续参与本项目的开发者和智能编码代理都必须遵守。

## 一、语言与文档

1. 项目文档必须使用中文编写。
2. 代码标识符、ROS 包名、节点名、话题名和参数名继续使用规范英文。
3. 新增功能时必须同步更新对应中文文档，不能只提交代码。
4. 命令必须提供可直接复制执行的完整版本，至少包含工作目录、环境加载、构建、启动、观察和停止步骤。

## 二、自底向上的开发顺序

项目严格按以下层次逐步推进：

1. 控制板、串口、电机、编码器和 IMU 等底层硬件驱动。
2. 底盘运动学、里程计、坐标轴方向与安全保护。
3. URDF、TF 和状态估计。
4. 雷达、建图、定位和 Nav2。
5. 感知、规划等通用算法。
6. 巡检任务、状态机、告警和数据上传等应用逻辑。

在下层没有形成明确测试结论前，不把上层问题混入下层测试脚本。

## 三、底层测试资产只增不删

1. 已经使用过的底层测试节点、脚本、配置和命令不得因为新增高层功能而删除。
2. 新实现不能直接覆盖旧测试入口。确需替代时，保留旧文件并标记“已停用”，同时记录替代原因和新入口。
3. 正式节点和测试节点必须分开，测试节点不得被正式 bringup 隐式启动。
4. 串口同一时间只能由一个进程打开。涉及串口的测试文档必须明确提醒关闭其他驱动节点。
5. 每个执行器测试都必须提供启动零输出、命令超时停车、退出停车和紧急断电说明。

## 四、测试记录维护规则

所有测试统一登记在 `docs/测试记录.md`，每项测试分配稳定编号，例如：

- `DRV-001`：串口与驱动层。
- `MOTOR-001`：电机层。
- `IMU-001`：惯导层。
- `ODOM-001`：里程计层。
- `TF-001`：TF 层。
- `NAV-001`：导航层。
- `APP-001`：应用层。

每个测试记录至少包含：

1. 测试目的与适用范围。
2. 当前硬件前提和安全条件。
3. 完整构建命令。
4. 完整启动命令。
5. 话题发布、查看和诊断命令。
6. 停止与恢复命令。
7. 预期结果。
8. 实际结果、测试日期和结论。
9. 对应代码与配置文件路径。
10. 已知问题和下一步。

后续测试按时间顺序追加新章节，不删除旧命令，不把历史失败结果改写成成功。修正错误时保留原记录，并追加“修订说明”。

## 五、硬件台账维护规则

硬件信息统一维护在 `docs/硬件台账.md`。

1. 用户每次反馈实物、接线、端口、型号、方向、尺寸、固件或测试结果后，都要更新硬件台账。
2. 信息分为“已确认”“待确认”“已停用”，不得把推测写成已确认事实。
3. 电机端口、编码器通道、轮子位置和正方向必须逐项记录。
4. 硬件变更必须追加变更记录，包含日期、变更前、变更后、影响范围和验证状态。
5. 与硬件相关的默认参数只有在实机确认后才能写入正式配置；未确认值应显式标注为临时值。

## 六、提交与验证要求

1. 修改底层代码后，至少执行对应单元测试、Python 静态检查和目标 ROS 包构建。
2. 没有实物参与的验证必须写成“离线验证”，不能表述为“实机通过”。
3. 实机测试结果必须由现场操作反馈确认，智能编码代理不能自行假定电机方向或传感器轴向正确。
4. 每次交付应说明：改了什么、保留了什么、验证了什么、还有什么未确认。

## 七、优先复用成熟开源方案

1. 设计和实现功能时，应优先调研并参考活跃维护、文档完善、许可证兼容的主流开源框架、算法或项目，避免在已有成熟方案可用时重复造轮子。
2. 引入或借鉴开源实现时，必须记录项目来源、固定版本或提交、许可证及本项目所做的适配，不得无来源复制代码或资产。
3. 只有在现有方案不能满足硬件约束、实时性、安全性或维护要求时才自行实现，并在对应中文设计或测试文档中说明取舍依据。
